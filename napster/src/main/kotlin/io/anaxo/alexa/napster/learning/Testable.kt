package io.anaxo.alexa.napster.learning

class Testable {
    fun getName(name: String): String {
        return name
    }

    fun multiplyIntegers(firstInteger: Int, secondInteger:Int):Int {
        return firstInteger * secondInteger
    }

    fun mockFunction(input: Int):Int {
        return input
    }
}
